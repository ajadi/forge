---
description: Manage feature branches, PR descriptions, and merge strategies instead of direct commits to main
argument-hint: "[branch name or PR description]"
---

$ARGUMENTS is an optional branch name or PR description.

Use the git-workflow agent to manage the branch-based workflow: create feature branch, write PR description, coordinate merge strategy.

Use when the project uses branch-based workflow instead of direct commits to main.
