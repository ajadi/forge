Run multi-perspective design panel via the consilium subagent.

$ARGUMENTS

Pass the task file path as argument (e.g. `/f-consilium tasks/TASK-042.md`).

Invoke the `consilium` subagent to run Architect + Security + DevOps perspectives and synthesize a design decision.
Use subagent: consilium
Mode: bypassPermissions